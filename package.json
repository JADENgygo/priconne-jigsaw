{
  "name": "priconne-jigsaw",
  "version": "1.0.0",
  "main": "index.js",
  "repository": "https://github.com/jadengygo/priconne-jigsaw.git",
  "author": "JADEN <29948746+JADENgygo@users.noreply.github.com>",
  "license": "MIT",
  "devDependencies": {
    "html-minifier": "^4.0.0",
    "reload": "^3.2.0"
  },
  "scripts": {
    "start": "reload -b",
    "minify": "rm -rf public && mkdir public && cp -R img public && html-minifier --collapse-whitespace --remove-comments --remove-optional-tags --remove-redundant-attributes --remove-script-type-attributes --remove-tag-whitespace --use-short-doctype --minify-css true --minify-js true -o public/index.html index.html"
  }
}
